[flake8]
exclude = .git,*migrations*,venv,dist,build,.tox,.pytest_cache
max-line-length = {{ cookiecutter.flake8_line_length }}
; F401: imported but unused
; W503: line break before binary operator
; W605 invalid escape sequence
